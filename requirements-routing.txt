# Compatibility alias for local cached OSM GraphML routing installs.
# OSMnx is now a pinned direct production dependency in requirements.txt,
# so this profile must not add a second unpinned declaration.
-r requirements.txt
